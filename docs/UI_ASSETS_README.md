# TempChain UI 자산

자산 위치: `erp/apps/web/public/ico/` (저장소 루트 기준).

회사 로고 PNG 6종과 현재 Vue 화면에서 참조하는 SVG 아이콘을 보관합니다.

- `logo/`: 가로 로고·세로 로고·심벌, 원색·흰색 변형
- `navigation/`, `actions/`: 현재 메뉴와 폼에서 사용하는 아이콘
- `LUCIDE_LICENSE.txt`: Lucide SVG 라이선스

Vue의 정적 자산 경로는 `/ico/`이며 파랑 `#134a83`·초록 `#619547` 로고 원본을 사용합니다.
